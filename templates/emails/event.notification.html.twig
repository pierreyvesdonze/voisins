<html>
	<head>
		<meta charset="UTF-8">
		<title>Nouvel événement</title>
	</head>
	{% apply inline_css %}
	<style>

		body {
			background-image: url("./assets/images/bg_body");
			color: #000;
		}
		h1 {
			color: #fff;
			font-size: 25px text-shadow: 2px 2px 2px rgba(0, 0, 0, 0.75);
		}
		p {
			font-size: 15px;
			color: black;
		}
	</style>

	<body>
		<div>
			<h1>Coucou !</h1>

			<p>
				{{ user.username }}
				a créé un nouvel événement !
				<a href="{{ absolute_url(path('event_view',{'id': event.id})) }}">Clique ici</a>
				pour le visualiser !
			</p>
		</div>
	</body>
	{% endapply %}

</html>