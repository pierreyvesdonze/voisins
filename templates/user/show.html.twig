{% extends 'base.html.twig' %}

{% block title %}Profil perso
{% endblock %}

{% block body %}
	<div class="avatar-profile">
		<img src="{{ asset('uploads/images/' ~ user.brochureFilename) }}" alt="avatar">
	</div>

	<table class="table">
		<tbody>
			<tr>
				<th>Pseudo</th>
				<td>{{ user.username }}</td>
			</tr>
			<tr>
				<th>Email</th>
				<td>{{ user.email }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('user_edit_profile') }}" class="btn btn-primary">edit</a>

	{{ include('user/_delete_form.html.twig') }}
{% endblock %}
