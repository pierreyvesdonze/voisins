{% extends "base.html.twig" %}


{% block body %}

	<h1>Événements passés</h1>

	<div class="archives">
		<a href="{{ path('event_list') }}">
			Retour aux listes
		</a>
	</div>

	<hr class="hr">

	<div class="flex-container">
		<div class="card-group">
			{% for event in events|sort((a, b)=> a.date <=> b.date) %}

				<div class="card" style="width: 18rem;">
					<div class="card-header">
						<span class="list-events">{{ event.type }}
						</span>
						<span>{{ event.user }}</span>
					</div>
					<div class="card-body">
						<h5 class="card-title">
							{{ event.title }}
						</h5>

					</div>
					<ul class="list-group list-group-flush">
						<li class="list-group-item">
							<span class="list-events">
								Lieu :
								{{ event.lieu }}
							</span>
						</li>
						<div class="date-item">
							<li class="list-group-item">
								<span class="date-events">
									{{ event.date|date("d/m/y à h:m") }}
								</span>
							</li>
						</div>
					</ul>
					<div class="card-body view-item">
						<a href="{{ path('event_view',{'id': event.id}) }}" class="btn btn-primary">Voir</a>
					</div>
				</div>

			{% endfor %}
		</div>
	</div>

{% endblock %}
